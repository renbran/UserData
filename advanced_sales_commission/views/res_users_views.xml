<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Inherit User Form View -->
        <record id="res_users_form_commission" model="ir.ui.view">
            <field name="name">res.users.form.commission</field>
            <field name="model">res.users</field>
            <field name="inherit_id" ref="base.view_users_form"/>
            <field name="arch" type="xml">
                <xpath expr="//page[@name='preferences']" position="after">
                    <page string="Commission" name="commission" groups="advanced_sales_commission.group_commission_manager">
                        <group>
                            <field name="is_commission_eligible"/>
                        </group>
                        <field name="commission_assignment_ids">
                            <tree editable="bottom">
                                <field name="config_id"/>
                                <field name="date_from"/>
                                <field name="date_to"/>
                                <field name="active" widget="boolean_toggle"/>
                            </tree>
                        </field>
                    </page>
                </xpath>
            </field>
        </record>
    </data>
</odoo>

